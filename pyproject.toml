[build-system]
requires = ["setuptools", "wheel", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "adept"
version = "0.1.2"
description = "Fast numerical library for Python"
requires-python = ">=3.8"